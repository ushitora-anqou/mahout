(*
 * This file has been generated by the OCamlClientCodegen generator for openapi-generator.
 *
 * Generated by: https://openapi-generator.tech
 *
 * Schema Io_k8s_api_core_v1_container_status.t : ContainerStatus contains details for the current status of this container.
 *)

type t = {
    (* AllocatedResources represents the compute resources allocated for this container by the node. Kubelet sets this value to Container.Resources.Requests upon successful pod admission and after successfully admitting desired pod resize. *)
    allocated_resources: Yojson.Safe.t list [@default []];
    (* ContainerID is the ID of the container in the format '<type>://<container_id>'. Where type is a container runtime identifier, returned from Version call of CRI API (for example \''containerd\''). *)
    container_id: string option [@default None];
    (* Image is the name of container image that the container is running. The container image may not match the image used in the PodSpec, as it may have been resolved by the runtime. More info: https://kubernetes.io/docs/concepts/containers/images. *)
    image: string;
    (* ImageID is the image ID of the container's image. The image ID may not match the image ID of the image used in the PodSpec, as it may have been resolved by the runtime. *)
    image_id: string;
    last_state: Io_k8s_api_core_v1_container_state.t option [@default None];
    (* Name is a DNS_LABEL representing the unique name of the container. Each container in a pod must have a unique name across all container types. Cannot be updated. *)
    name: string;
    (* Ready specifies whether the container is currently passing its readiness check. The value will change as readiness probes keep executing. If no readiness probes are specified, this field defaults to true once the container is fully started (see Started field).  The value is typically used to determine whether a container is ready to accept traffic. *)
    ready: bool;
    resources: Io_k8s_api_core_v1_resource_requirements.t option [@default None];
    (* RestartCount holds the number of times the container has been restarted. Kubelet makes an effort to always increment the value, but there are cases when the state may be lost due to node restarts and then the value may be reset to 0. The value is never negative. *)
    restart_count: int32;
    (* Started indicates whether the container has finished its postStart lifecycle hook and passed its startup probe. Initialized as false, becomes true after startupProbe is considered successful. Resets to false when the container is restarted, or if kubelet loses state temporarily. In both cases, startup probes will run again. Is always true when no startupProbe is defined and container is running and has passed the postStart lifecycle hook. The null value must be treated the same as false. *)
    started: bool option [@default None];
    state: Io_k8s_api_core_v1_container_state.t option [@default None];
} [@@deriving yojson { strict = false }, show ];;

(** ContainerStatus contains details for the current status of this container. *)
let create (image : string) (image_id : string) (name : string) (ready : bool) (restart_count : int32) : t = {
    allocated_resources = [];
    container_id = None;
    image = image;
    image_id = image_id;
    last_state = None;
    name = name;
    ready = ready;
    resources = None;
    restart_count = restart_count;
    started = None;
    state = None;
}

